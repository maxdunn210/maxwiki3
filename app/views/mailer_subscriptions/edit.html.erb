<%-
  @title = 'Mailer Subscriptions'
  @left_column_partial = 'reg_admin/reg_admin_left_menu' 
-%>

<h1>Edit Mailer Subscription</h1>

<%= form_tag :controller => 'mailer_subscriptions', :action => 'update', :id => @mailer_subscription do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Save' %>
<% end %>

<table>
	<tr>
		<td>
		<%= form_tag :controller => 'mailer_subscriptions', :action => 'index' do %> 
		  <%= submit_tag "Cancel" %>
		<% end %>
		</td>
		
		<td>
		<%= form_tag :controller => 'mailer_subscriptions', :action => 'destroy', :id => @mailer_subscription do %>
		  <%= submit_tag "Delete" %>
		  &nbsp;
		<% end %>
		</td>
	</tr>
</table>
